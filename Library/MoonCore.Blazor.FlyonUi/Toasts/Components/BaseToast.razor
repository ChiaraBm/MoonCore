@code
{
    public ToastLauncher Launcher { get; set; }
    public ToastReference Reference { get; set; }

    /// <summary>
    /// Hides/removes this toast component
    /// </summary>
    protected async Task HideAsync() => await Launcher.CloseAsync(Reference);
}
