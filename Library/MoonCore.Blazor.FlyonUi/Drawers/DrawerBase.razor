@code
{
    [Parameter] public DrawerReference Reference { get; set; }
    [Parameter] public DrawerLauncher Launcher { get; set; }

    /// <summary>
    /// Closes the current drawer
    /// </summary>
    public async Task CloseAsync()
        => await Launcher.CloseAsync(Reference);
}
